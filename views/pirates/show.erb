<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1>Name: <%= @pirate.name%></h1>
    <h3>Weight: <%= @pirate.weight%></h3>
    <h3>Height: <%= @pirate.height%></h3>

    <% @ships.each do |ship|%>
      <p> <%= ship.name%></p>
      <p> <%= ship.type%></p>
      <p> <%= ship.booty%></p>
    <%end%>

  </body>
</html>
